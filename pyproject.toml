[project]
name = "ai-agent-langgraph-chromadb"
version = "0.1.0"
description = "AI Agent using LangGraph and ChromaDB"
readme = "README.md"
requires-python = ">=3.11"

[tool.uv]
dev-dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
